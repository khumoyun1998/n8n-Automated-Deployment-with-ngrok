# Copy this file to .env before deploying.
# deploy.sh fills these in automatically from the static `domain:` set in
# ngrok.yml, so you normally don't edit them by hand — just set your reserved
# domain in ngrok.yml. Leave them empty here.
WEBHOOK_URL=
N8N_EDITOR_BASE_URL=
N8N_HOST=
